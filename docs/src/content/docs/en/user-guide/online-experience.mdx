---
title: Online Experience
description: "Try Vide's core RTL IDE features in the browser without installing the local extension."
---

import VideLab from '../../../../components/VideLab.astro';
import {
  completionFiles,
  editAidFiles,
  navigationFiles,
  structureFiles,
} from '../../../../examples/dailyUse';

The examples below are small RTL projects that run directly in the browser. Without extra configuration, you can try symbol navigation, code insight, precise completion, and automatic refactoring.

When an example first loads, wait for the circle indicator in the upper-right corner of the editor to turn green. Then start from the selected code position. You can edit RTL, switch files, open the diagnostics panel, and use the reset button in the toolbar when you want to restore the initial state.

## Symbol Navigation

In traditional Verilog projects, finding where a variable is defined or used often means running a global Ctrl+F string search. That search also returns similar names and identically named symbols from unrelated scopes, which quickly gets in the way.

In Vide, Go to Definition, Find References, and Document Symbols help you move between modules, ports, and registers without leaving the current context, so you can trace RTL connections more directly.

This example demonstrates Go to Definition and Find References.

### Steps

1. Load the example. `u_and_gate` is already selected in `top.v`.
2. Run `Go to Definition` on `u_and_gate` or `and_gate` to jump to `and_gate.v`.
3. Return to `top.v` and inspect references or highlights on `gate_out`.

<VideLab
  projectId="online-navigation-en"
  projectLabel="Symbol Navigation"
  entryFile="top.v"
  files={navigationFiles}
  load="visible"
  activeFile="top.v"
  selection="8:5-8:13"
  title="Jump from an instance to its module definition"
  description="Load the example, then run Go to Definition on u_and_gate to jump to and_gate.v."
/>

## Code Insight

In a typical editing flow, checking module signatures and port connections often means jumping back and forth between files. Even converting a numeric literal between bases can require an external tool. These interruptions make it harder to stay focused on the RTL design.

Vide provides hover information and code annotations at the edit location, so you can inspect modules, literals, and port connections in one window with fewer context switches.

This example demonstrates hover information for module signatures and instances, plus annotations for port names.

### Steps

1. Load the example and read the port annotations next to each `u_mux` connection.
2. Hover over `mux2` or `u_mux` to inspect module and instance information.
3. Open the `mux2` definition and compare its port list with the instance connections.

<VideLab
  projectId="online-insight-en"
  projectLabel="Code Insight"
  entryFile="inlay_hints.v"
  files={structureFiles}
  load="visible"
  activeFile="inlay_hints.v"
  selection="18:5-23:6"
  title="Inspect port-connection annotations"
  description="Load the example, then read the port annotations around u_mux or hover over mux2 / u_mux."
/>

## Precise Completion

Writing RTL without completion depends heavily on memory: language syntax, project structure, module ports, and signal names. If completion only does string matching, suggestions are easily cluttered with similar symbols and do little to speed up editing.

Vide's completion understands the current code context. It provides project-aware suggestions for instantiations, port connections, and other editing positions, and uses snippets for structured edits.

### Steps

1. Load the example and place the cursor inside the `adder8 u0(...)` port list.
2. Delete a port name, or type `.` on a new line and trigger completion.
3. Select a port candidate, then fill in the signal inside the parentheses.

<VideLab
  projectId="online-completion-en"
  projectLabel="Precise Completion"
  entryFile="completion.v"
  files={completionFiles}
  load="visible"
  activeFile="completion.v"
  selection="16:5-16:12"
  title="Trigger completion at a port connection"
  description="Load the example, type . inside the u0 port list, and trigger completion."
/>

## Automatic Refactoring

Refactoring RTL often involves repetitive work: filling missing ports one by one during module instantiation, converting ordered port connections to named connections, or renaming symbols across declarations and uses. These mechanical edits are exactly the kind of detail the editor can handle accurately.

Vide provides automatic refactoring and rename support so the editor can handle those mechanical details for you.

This example demonstrates missing-port completion and rename.

### Steps

1. Load the example, then manually remove one port connection from `u_counter`.
2. Place the cursor near the `u_counter` instance and open the code action menu to complete the missing connection.
3. Rename `smp_cnt` and check that the declaration, connection, and usage update together.

<VideLab
  projectId="online-refactor-en"
  projectLabel="Automatic Refactoring"
  entryFile="code_action_rename.v"
  files={editAidFiles}
  load="visible"
  activeFile="code_action_rename.v"
  selection="22:16-22:23"
  title="Complete ports and rename a symbol"
  description="Load the example, remove one port connection, then open code actions or rename smp_cnt."
/>

## Next Step

After these examples, you should have a clearer sense of how Vide helps with RTL editing. For more flexible online exploration, open the [Playground](../../playground/) and use it together with [Configure the First Project](../first-project/) and [Features](../features/).

To use Vide in a local project, first follow the [Installation Guide](../installation/) for your editor, then write the first `vide.toml` with [Configure the First Project](../first-project/) for more accurate analysis.
