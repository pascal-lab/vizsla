---
title: "VS Code Status Bar, Command Palette, and Logs"
description: Status bar items, Command Palette commands, and log entry points for the Vide VS Code extension.
---

import Codicon from '../../../../components/Codicon.astro';

This page lists the status bar items, Command Palette commands, and log entry points provided by the Vide VS Code extension.

## Status Bar

The main status item is named `Vide` and appears on the right side of the VS Code status bar. It reports language server and project configuration state:

| State | Meaning |
| --- | --- |
| `Vide` | The server has started; hover to see project configuration state. |
| <Codicon name="sync" modifier="spin" label="Sync spin icon" /> `Vide` | Server startup, shutdown, or project configuration loading is in progress. |
| <Codicon name="warning" label="Warning icon" /> `Vide` | Usually means the current workspace has no project configuration file. |
| <Codicon name="error" label="Error icon" /> `Vide` | Server startup or project configuration loading failed. |

Click the `Vide` status item or run `Vide: Show Status` to open the status menu. Menu items vary with project state; common actions include opening or creating the project configuration file, profiling diagnostics, reloading the project, restarting the language server, and showing output.

When Qihe analysis runs, a separate `Qihe` status item appears. If Qihe fails, clicking that item opens the `Vide Qihe` output channel.

If the status bar shows <Codicon name="warning" label="Warning icon" /> or <Codicon name="error" label="Error icon" />, start with `Vide: Show Status` and `Vide: Show Language Server Output`. For deeper investigation, continue with [Troubleshooting and Bug Reports](../../advanced-guide/troubleshooting/).

## Command Palette Commands

Press `F1`, or use `Ctrl+Shift+P` / `Cmd+Shift+P`, to open the Command Palette in VS Code. The table below lists the commands contributed by Vide. In normal use, look at the Command Palette title; command IDs are mainly for keybindings or scripts.

| Command ID | Command Palette title | Purpose |
| --- | --- | --- |
| `vide.showOutput` | `Vide: Show Language Server Output` | Opens the `Vide Language Server` output channel. |
| `vide.restartServer` | `Vide: Restart Language Server` | Stops and restarts the current language server. |
| `vide.showServerVersion` | `Vide: Show Server Version` | Shows the current extension version, then runs the current server command with the current cwd and environment, combines `vide.server.args` with `--version`, and does not append `vide.server.additionalArgs`. |
| `vide.reloadWorkspace` | `Vide: Reload Project Configuration` | Rereads project configuration files and refreshes project information without restarting the server. |
| `vide.showStatus` | `Vide: Show Status` | Opens the Vide status menu. |
| `vide.runQiheAnalysis` | `Vide: Run Qihe Analysis` | Runs Qihe analysis for the current local Verilog/SystemVerilog file. |
| `vide.profileDiagnostics` | `Vide: Profile Diagnostics` | Starts a temporary language server and runs one diagnostics profiling pass for the workspace or current file. |

## Logs

Vide writes runtime information to VS Code output channels. You can open them from the status menu through `Show Output`, or through the related Command Palette commands. The table below lists what each output channel contains.

| Output channel | Records |
| --- | --- |
| `Vide Language Server` | Extension activation, platform, VS Code version, server command, args, cwd, bundled server lookup result, start/stop/restart, and version queries. |
| `Vide Qihe` | Target file, command progress, Qihe output, and failure details for `vide.runQiheAnalysis`. |
| `Vide Profiling` | Target, artifact directory, diagnostics request time, and generated file paths for `vide.profileDiagnostics`. |

## Advanced: Diagnostics Profiling Artifacts

`Vide: Profile Diagnostics` is mainly for large-project troubleshooting. It generates:

| File | Description |
| --- | --- |
| `trace.json` | Chrome/Perfetto/Speedscope-compatible trace; the bundled Speedscope viewer opens this file. |
| `summary.json` | Request timing, diagnostics summary, and top span summary. |
| `trace.folded` | Folded stack generated from the trace. |
| `flamegraph.svg` | Static flamegraph. |
| `server.log` | Temporary language server log. |

Profiling uses an isolated temporary language server process, so it does not restart or affect the language server used by your editor session.
