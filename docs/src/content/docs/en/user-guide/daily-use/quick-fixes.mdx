---
title: Quick Fixes and Rename
description: Use port/parameter fixes, diagnostic quick fixes, and rename.
---

import FeatureExample from '../../../../../components/FeatureExample.astro';
import VideLab from '../../../../../components/VideLab.astro';
import codeActionRenameGif from '../../../assets/features/code-action-rename.gif';
import inlayHintsGif from '../../../assets/features/inlay-hints.gif';
import { editAidFiles, structureFiles } from '../../../../../examples/dailyUse';

Vide provides code actions near instance ports, parameters, selected diagnostics, and symbols that can be renamed. Place the cursor on the relevant code and open the VS Code lightbulb menu to see the available fix or refactor.

Quick fixes mostly fall into two groups: repairs that complete or rewrite ports/parameters from diagnostics or instance structure, and local refactors that do not require diagnostics, such as integer literal base conversion. Renameable symbols use VS Code's `Rename Symbol` command.

Related settings reference: [Diagnostics](../../../advanced-guide/vscode-settings/#diagnostics).

<FeatureExample
  image={inlayHintsGif}
  imageAlt="Animation showing VS Code converting ordered port connections to named connections from inlay hints"
  imageCaption="Convert port connections from inlay hints"
  labCaption="The browser example reuses the same mux demo"
>
  <VideLab
    projectId="daily-port-connection-fix"
    projectLabel="Features: Port Connection Fixes"
    entryFile="inlay_hints.v"
    files={structureFiles}
    height="100%"
    initialActiveFile="inlay_hints.v"
    initialSelection="18:5-23:6"
    title="Convert Ordered Port Connections to Named Connections"
    description="The recording triggers the conversion by double-clicking port inlay hints. You can also place the cursor near the instance and run the ordered-to-named port connection code action from the lightbulb menu."
  />
</FeatureExample>

<FeatureExample
  image={codeActionRenameGif}
  imageAlt="Animation showing code actions and rename in VS Code"
  imageCaption="Code actions and rename in VS Code"
  labCaption="The browser example reuses the same counter demo"
>
  <VideLab
    projectId="daily-edit-aids"
    projectLabel="Features: Edit Aids"
    entryFile="code_action_rename.v"
    files={editAidFiles}
    height="100%"
    initialActiveFile="code_action_rename.v"
    initialSelection="22:16-22:23"
    title="Code Actions and Rename"
    description="This example uses the counter/top demo. Try opening the lightbulb menu near the instance, or use Find References before renaming smp_cnt."
  />
</FeatureExample>

## FAQ

### Instance Ports or Parameters Report Errors

Place the cursor near the instance and open the lightbulb menu. Vide currently supports these common fixes:

- Missing port connections: use `Fill connections`.
- Parameters without values: use `Fill parameters`.
- Mixed ordered and named port connections: use `Convert ordered port connections to named connections`, or `Remove empty port connections` for extra empty connections.
- Mixed ordered and named parameter assignments: use `Convert ordered parameter assignments to named assignments`.
- A `.port` shorthand missing explicit `()`: use `Add explicit empty port connections`.
- An instance with no port list: use `Add empty instance port list`.
- Integer literal base is inconvenient: use `Convert literal to binary`, `Convert literal to octal`, `Convert literal to decimal`, or `Convert literal to hexadecimal` near the number.

If these actions are missing, confirm that Vide can resolve the target module first. A direct check is `Go to Definition` on the instance module name. If it does not jump, fix `sources`, `include_dirs`, `defines`, or `libraries` first.

### Check Scope Before Rename

Rename depends on symbol resolution. Before a large rename, run `Find References` and confirm the result set. If references are missing, update project configuration and reload it first.
