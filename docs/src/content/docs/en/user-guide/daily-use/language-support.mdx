---
title: Language Support
description: Check how Vide recognizes Verilog/SystemVerilog files and where basic editor features end.
---

import FeatureExample from '../../../../../components/FeatureExample.astro';
import VideLab from '../../../../../components/VideLab.astro';
import semanticTokensImage from '../../../assets/features/semantic-tokens.png';
import { semanticTokenFiles } from '../../../../../examples/dailyUse';

The VS Code extension registers two language modes:

| Language | File extensions |
| --- | --- |
| Verilog | `.v`, `.vh` |
| SystemVerilog | `.sv`, `.svh`, `.svi` |

After you open these files, VS Code applies basic syntax highlighting, comment rules, bracket matching, and file icons. These do not depend on the background language server; they should work even while the server is still starting.

Diagnostics, go to definition, completion, hover, signature help, inlay hints, semantic highlighting, outline, folding, selection ranges, and instance count lens entries become available after the language server starts. Cross-file understanding also needs enough project configuration at the workspace root.

Related settings reference: [Semantic Tokens](../../../advanced-guide/vscode-settings/#semantic-tokens), [Files](../../../advanced-guide/vscode-settings/#files).

## What Semantic Highlighting Adds

Syntax highlighting only knows lexical information such as keywords, numbers, and comments. Semantic highlighting uses language-server analysis. When your theme supports semantic tokens, Vide can additionally mark port direction, clock/reset ports, and symbol read/write positions.

The useful cues are small but practical:

- `input` and `output` ports can use different colors for signal direction.
- clk/rst ports can stand out, making timing boundaries easier to scan in module interfaces.
- Read and write positions can be styled differently, which helps when checking where a signal is assigned or consumed.

<FeatureExample
  image={semanticTokensImage}
  imageAlt="VS Code screenshot showing semantic color differences for input/output, clk/rst, and read/write positions"
  imageCaption="Semantic highlighting in VS Code"
  labCaption="The example covers port direction, clk/rst, and read/write positions"
>
  <VideLab
    projectId="daily-semantic-tokens"
    projectLabel="Features: Semantic Tokens"
    entryFile="semantic-token-demo.v"
    files={semanticTokenFiles}
    height="100%"
    initialActiveFile="semantic-token-demo.v"
    initialSelection="1:1-18:10"
    title="Semantic Tokens Example"
    description="The example covers input/output ports, clk/rst ports, and signal read/write positions so it can be compared with the semantic colors in the screenshot."
  />
</FeatureExample>

## FAQ

### The File Is Not Recognized

Check the language mode in the lower-right corner of VS Code. If `.v` / `.vh` files are not Verilog, or `.sv` / `.svh` / `.svi` files are not SystemVerilog, switch the language mode manually once and check whether another extension has overridden the association.

If syntax highlighting works but navigation, diagnostics, and completion do not, this is usually a language-server or project-configuration issue. Click the `Vide` status item first; if it reports no project manifest, create or update `vide.toml` using [First Project](../../first-project/).

### Header Files Have Fewer Features

`.vh`, `.svh`, and `.svi` files usually participate through the `.v` or `.sv` file that includes them. When you open a header by itself, syntax highlighting and local editing features should work, but full cross-file diagnostics may not run standalone. Put the including source files in `sources` and the header directory in `include_dirs` for behavior closer to your real build.
