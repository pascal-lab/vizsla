---
title: Navigation and Reading
description: Use definition, references, hover, and document highlights to read cross-file RTL code.
---

import FeatureExample from '../../../../../components/FeatureExample.astro';
import VideLab from '../../../../../components/VideLab.astro';
import gotoDefGif from '../../../assets/features/goto-def.gif';
import hoverGif from '../../../assets/features/hover.gif';
import { navigationFiles } from '../../../../../examples/dailyUse';

Vide provides these reading features:

- `Go to Definition` / `Go to Declaration`: jump to symbols, modules, ports, parameters, or instantiated targets.
- `Find References`: find symbol references.
- Hover: inspect signatures for modules, ports, parameters, tasks, and functions.
- Document Highlight: highlight uses of the same symbol in the current file.
- `Go to Declaration` prefers declaration locations; when there is no separate declaration, it falls back to definition results.

Related settings reference: [Scope](../../../advanced-guide/vscode-settings/#scope), [Semantic Tokens](../../../advanced-guide/vscode-settings/#semantic-tokens).

<FeatureExample
  image={gotoDefGif}
  imageAlt="Animation showing VS Code jumping from top to the and_gate definition"
  imageCaption="Cross-file navigation in VS Code"
  labCaption="Try the same jump in the browser"
>
  <VideLab
    projectId="daily-navigation"
    projectLabel="Features: Navigation"
    entryFile="top.v"
    files={navigationFiles}
    height="100%"
    initialActiveFile="top.v"
    initialSelection="8:5-8:13"
    title="Jump from Instance to Module Definition"
    description="Try Go to Definition on and_gate. The target module lives in another file, and the project is provided directly by this docs page."
  />
</FeatureExample>

<FeatureExample
  image={hoverGif}
  imageAlt="Animation showing module hover information in VS Code"
  imageCaption="Hover information in VS Code"
  labCaption="Inspect hover on the same instance in the browser"
>
  <VideLab
    projectId="daily-hover"
    projectLabel="Features: Hover"
    entryFile="top.v"
    files={navigationFiles}
    height="100%"
    initialActiveFile="top.v"
    initialSelection="8:5-8:13"
    title="Inspect Instance Target Information"
    description="Place the cursor on and_gate to inspect the module signature. Hover information comes from target-module resolution in the current project view."
  />
</FeatureExample>

## Project Scope

Cross-file navigation needs the target file to be part of the project view. The important fields are:

- `sources` matches the file that defines the target module.
- `include_dirs` includes the include directory.
- Macro definitions configured through `defines` enable the conditional-compilation branch that contains the target; modules hidden behind inactive `` `ifdef `` branches are not analyzed in the current project view.
- `libraries` includes the dependency project.

See [Project Configuration](../../project-configuration/) for the fields.

:::caution[Duplicate modules]
Best-effort indexing can make a nearest-candidate guess for reading, but this is not a SystemVerilog language rule. In real projects, handle duplicate module names through explicit project configuration, library boundaries, or your build scripts. For the exact behavior without full configuration, see [Navigation and Duplicate Modules](../../../advanced-guide/parsing-and-analysis/#navigation-and-duplicate-modules).
:::

:::note[References and rename]
References and rename use the same project view. With only one file open, Vide may only handle that file or locally indexed information; add the related sources to `sources`, then run `Vide: Reload Project Configuration`.
:::
