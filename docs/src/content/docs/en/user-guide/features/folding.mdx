---
title: Folding
description: Fold modules, port lists, declarations, block statements, and comments by SystemVerilog syntax structure.
---

import VideLab from '../../../../../components/VideLab.astro';
import { inlayHintFiles } from '../../../../../examples/dailyUse';

## What It Is

Folding lets the editor collapse code by structure. Vide provides folding ranges for multi-line comments, regions, modules, port lists, declarations, generate/specify constructs, instances, continuous assignments, block statements, and ordinary statements.

During development, folding lets you hide less relevant parts of the code and keep the editor view cleaner.

## Demo

You can try folding code freely in the browser.

<VideLab
  projectId="daily-folding"
  projectLabel="Features: Folding"
  entryFile="inlay_hints.v"
  files={inlayHintFiles}
  initialActiveFile="inlay_hints.v"
  initialSelection="1:1-19:10"
  title="Fold by SystemVerilog structure"
  description="Use VS Code folding controls to collapse modules, port lists, and instance connections."
/>

## Project Configuration

### Without Complete Configuration

Folding mainly comes from syntax and structure in the current file, so it works in single-file cases.

### With Complete Configuration

After `vide.toml` is configured, include files and macro branches are parsed from the project configuration, and related structure ranges update accordingly.
