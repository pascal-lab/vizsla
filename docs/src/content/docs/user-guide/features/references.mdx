---
title: 引用搜索
description: 查找模块、端口、参数和信号在工程中的使用位置。
---

import FeatureExample from '../../../../components/FeatureExample.astro';
import VideLab from '../../../../components/VideLab.astro';
import findAllReferencesImage from '../../assets/features/find-all-references.png';
import { usageFiles } from '../../../../examples/dailyUse';

## 什么是引用搜索

引用搜索能够找到选中的符号在哪里被使用。在 SystemVerilog 工程里，它可以用于追模块实例、端口连接、参数和信号的使用位置，也可以配合定义跳转确认一个连接关系是否完整。

你可以在 VS Code 中执行 `查找所有引用` 或 `查看引用`。Vide 会查找对应符号的使用位置，去重后显示在引用视图里。

## 演示

<FeatureExample
  image={findAllReferencesImage}
  imageAlt="VS Code 中查找 Verilog 符号引用的截图"
  imageCaption="VS Code 中的引用搜索"
  labCaption="在浏览器里搜索同一符号"
>
  <VideLab
    projectId="daily-references"
    projectLabel="Features: References"
    entryFile="usage.v"
    files={usageFiles}
    height="100%"
    initialActiveFile="usage.v"
    initialSelection="26:10-26:25"
    title="查找信号引用"
    description="加载后在 one_cycle_pulse 上执行查找所有引用，查看声明、端口连接和 assign 使用位置。"
  />
</FeatureExample>

## 项目配置影响

### 没有完整配置时

Vide 会在工作区扫描到的所有 Verilog/SystemVerilog 文件中查找引用。这个结果适合快速阅读代码，但如果工程依赖 include、宏分支或外部库，结果可能不完整。

### 有完整配置时

写好 `vide.toml` 后，引用搜索会按 `sources` 和 `libraries` 描述的工程范围执行，并结合 include 目录和宏定义判断实际参与分析的文件。

## 结果显示

默认情况下，引用结果会把声明位置一起显示，方便你从列表里确认符号是从哪里引入的。如果你只想看真正的使用位置，可以在 VS Code Settings 中写：

```json
{
  "vide.references.includeDeclaration": false
}
```

完整参考见 [Scope 设置](../../vscode-settings/#scope)。
