---
title: 导航和阅读
description: 使用跳转、引用、悬停和文档高亮，并处理跳转失败或同名模块歧义。
---

import FeatureExample from '../../../../components/FeatureExample.astro';
import VideLab from '../../../../components/VideLab.astro';
import gotoDefGif from '../../assets/features/goto-def.gif';
import hoverGif from '../../assets/features/hover.gif';
import { navigationFiles } from '../../../../examples/dailyUse';

Vide 提供这些阅读能力：

- `转到定义` / `转到声明`：跳到符号、模块、端口、参数或实例目标。
- `查找所有引用` / `查看引用`：查找符号引用。
- 悬停或 `显示或聚焦悬停`：查看模块、端口、参数、任务/函数等签名信息。
- 文档高亮：高亮当前文件里同一符号的使用位置。
- `转到声明` 会优先跳到声明位置；当没有单独声明位置时，会退回定义跳转结果。

相关设置参考：[Scope](../../../advanced-guide/vscode-settings/#scope)、[Semantic Tokens](../../../advanced-guide/vscode-settings/#semantic-tokens)。

<FeatureExample
  image={gotoDefGif}
  imageAlt="VS Code 中从 top 模块跳转到 and_gate 定义的动图"
  imageCaption="VS Code 中的跨文件跳转"
  labCaption="示例可尝试同一个跳转"
>
  <VideLab
    projectId="daily-navigation"
    projectLabel="Features: Navigation"
    entryFile="top.v"
    files={navigationFiles}
    height="100%"
    initialActiveFile="top.v"
    initialSelection="8:5-8:13"
    title="从实例跳转到模块定义"
    description="在 and_gate 上执行转到定义；目标模块在另一个文件里，项目由当前页面直接提供。"
  />
</FeatureExample>

<FeatureExample
  image={hoverGif}
  imageAlt="VS Code 中查看模块悬停信息的动图"
  imageCaption="VS Code 中的悬停信息"
  labCaption="示例可在同一实例上查看悬停信息"
>
  <VideLab
    projectId="daily-hover"
    projectLabel="Features: Hover"
    entryFile="top.v"
    files={navigationFiles}
    height="100%"
    initialActiveFile="top.v"
    initialSelection="8:5-8:13"
    title="查看实例目标信息"
    description="把光标放在 and_gate 上查看模块签名；如果悬停没有信息，通常也意味着目标模块没有被当前项目视图解析到。"
  />
</FeatureExample>

## 常见问题

### 跳不到定义

先在实例模块名上执行 `转到定义`。如果模块名本身跳不过去，通常是项目配置没有把目标文件加载进来：

- `sources` 是否匹配目标模块所在文件。
- `include_dirs` 是否包含 include 目录。
- 宏定义，也就是 `defines`，是否启用了目标代码所在的条件编译分支；如果目标 module 被 `` `ifdef `` 屏蔽，当前项目视图不会分析它。
- `libraries` 是否包含外部依赖工程。

配置方式见 [项目配置](../../project-configuration/)。

### 同名模块跳转不符合预期

尽力索引会为了阅读体验做一次就近推测，但这不是 SystemVerilog 的语言规则。正式工程里，同名 module 应该通过明确项目配置、库边界或编译脚本处理。想了解 Vide 在无配置项目中的具体取舍，见 [分析模型](../../../advanced-guide/parsing-and-analysis/#跳转和同名模块)。

### 引用或重命名范围太小

确认当前文件属于同一个项目视图。只靠单文件打开时，Vide 可能只能处理当前文件或已索引到的局部信息。把相关源文件写进 `sources` 后，再重新加载项目配置。
