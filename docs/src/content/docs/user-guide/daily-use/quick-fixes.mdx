---
title: 快速修复与重命名
description: 使用端口/参数快速修复、诊断快速修复和重命名。
---

import FeatureExample from '../../../../components/FeatureExample.astro';
import VideLab from '../../../../components/VideLab.astro';
import codeActionRenameGif from '../../assets/features/code-action-rename.gif';
import inlayHintsGif from '../../assets/features/inlay-hints.gif';
import { editAidFiles, structureFiles } from '../../../../examples/dailyUse';

Vide 会在实例端口、参数、部分诊断和可重命名符号附近提供代码操作。把光标放到相关位置后，打开 VS Code 灯泡菜单即可查看当前可用的修复或重构。

相关设置参考：[Diagnostics](../../../advanced-guide/vscode-settings/#diagnostics)。

<FeatureExample
  image={inlayHintsGif}
  imageAlt="VS Code 中双击行内提示将有序端口连接转换为命名连接的动图"
  imageCaption="从行内提示转换端口连接"
  labCaption="示例复用同一个 mux demo"
>
  <VideLab
    projectId="daily-port-connection-fix"
    projectLabel="Features: Port Connection Fixes"
    entryFile="inlay_hints.v"
    files={structureFiles}
    height="100%"
    initialActiveFile="inlay_hints.v"
    initialSelection="18:5-23:6"
    title="有序端口连接转命名连接"
    description="录屏里双击端口行内提示触发转换；也可以在实例附近打开灯泡菜单，执行将有序端口连接转换为命名连接。"
  />
</FeatureExample>

<FeatureExample
  image={codeActionRenameGif}
  imageAlt="VS Code 中使用代码操作和重命名的动图"
  imageCaption="VS Code 中的代码操作和重命名"
  labCaption="示例复用同一个 counter demo"
>
  <VideLab
    projectId="daily-edit-aids"
    projectLabel="Features: Edit Aids"
    entryFile="code_action_rename.v"
    files={editAidFiles}
    height="100%"
    initialActiveFile="code_action_rename.v"
    initialSelection="22:16-22:23"
    title="代码操作和重命名"
    description="这个示例使用 counter/top demo。试试在实例附近打开灯泡菜单，或先用查找所有引用检查 smp_cnt 的重命名范围。"
  />
</FeatureExample>

## 实例端口或参数报错

先把光标放到对应实例附近，打开灯泡菜单。Vide 目前支持这些常见修复：

- 端口没有接全：使用 `补全连接`。
- 参数没有值：使用 `补全参数`。
- 端口混用了有序写法和命名写法：使用 `将有序端口连接转换为命名连接`，或用 `移除空端口连接` 清理多余空连接。
- 参数混用了有序写法和命名写法：使用 `将有序参数赋值转换为命名赋值`。
- 写了 `.port` 但缺少 `()`：使用 `添加显式空端口连接`。
- 实例完全没有端口列表：使用 `添加空实例端口列表`。

如果灯泡里没有这些操作，先确认目标模块能被 Vide 找到。最直接的检查方式是在实例模块名上执行 `转到定义`；跳不过去时，优先修正 `sources`、`include_dirs`、`defines` 或 `libraries`。

## 重命名前先确认范围

重命名依赖符号解析结果。大范围重命名前，先用 `查找所有引用` 看结果是否符合预期；如果引用结果缺文件，先补项目配置并重新加载。
