---
title: VS Code 设置参考
description: Vide VS Code 扩展的紧凑配置项参考。
---

import VscodeSettingsTable from '../../../components/VscodeSettingsTable.astro';
import ThinLinkCard from '../../../components/ThinLinkCard.astro';

所有设置都在 `vide.*` 命名空间下。可以在 VS Code 设置界面搜索 `Vide`，也可以直接编辑 `settings.json`。

## 常用设置速查

多数情况下只需要关注这些：

| 你想做什么 | 常用设置 |
| --- | --- |
| 让 VS Code 调用本机的骑河（Qihe） | `vide.qihe.command` |
| 指定本机的 `verible-verilog-format` | `vide.formatter.path` |
| 控制诊断刷新是保存后还是输入时 | `vide.diagnostics.update` |
| 开关端口、参数、结构结尾的行内提示 | `vide.inlayHints.*` |
| 开关模块声明上方的实例数量提示 | `vide.lens.instantiations.enable` |
| 项目配置变化后是否自动刷新 | `vide.workspace.auto.reload` |

服务器启动命令、文件监听、诊断规则和通信跟踪更偏排障或开发场景；日常使用保持默认值即可。

## Server

这组设置用于替换或调试后台语言服务器。日常使用保持默认值即可。

<VscodeSettingsTable
  locale="zh"
  settings={[
    'vide.server.command',
    'vide.server.args',
    'vide.server.additionalArgs',
    'vide.server.cwd',
    'vide.trace.server',
  ]}
/>

这些服务器启动设置变更后，扩展会提示 `重启`：`vide.server.command`、`vide.server.args`、`vide.server.additionalArgs`、`vide.server.cwd`、`vide.trace.server`。

示例：

```json
{
  "vide.server.command": "D:\\tools\\vide\\vide.exe",
  "vide.server.additionalArgs": ["--log", "debug"]
}
```

## Qihe

运行 `Vide：运行 Qihe 分析` 时才需要看这组设置。

对应日常使用页：[骑河（Qihe）](../../user-guide/daily-use/qihe/)。

<ThinLinkCard
  href="https://qihe.pascal-lab.net/"
  title="骑河（Qihe）项目主页"
  action="打开"
>
  了解骑河（Qihe）的获取方式和项目入口。
</ThinLinkCard>

<ThinLinkCard
  href="https://qihe-docs.pascal-lab.net/platform-cli/getting-started.html"
  title="骑河（Qihe）命令行快速开始"
  action="打开"
>
  配置 VS Code 前，先确认本机 <code>qihe</code> 命令可用。
</ThinLinkCard>

<VscodeSettingsTable
  locale="zh"
  settings={[
    'vide.qihe.command',
    'vide.qihe.autoConfigureArgsFromManifest',
    'vide.qihe.compileArgs',
    'vide.qihe.runArgs',
  ]}
/>

`Vide：运行 Qihe 分析` 只对本地 Verilog/SystemVerilog 文件可用。默认会从当前 `vide.toml` 推导骑河（Qihe）编译模式、顶层模块、include 目录和宏定义。如果项目已经用脚本管理这些参数，关闭自动推导并显式配置 `compileArgs` / `runArgs`。

示例：

```json
{
  "vide.qihe.command": "D:\\tools\\qihe\\qihe.exe",
  "vide.qihe.autoConfigureArgsFromManifest": false,
  "vide.qihe.compileArgs": ["--mode", "sv", "--", "-I", "include"],
  "vide.qihe.runArgs": ["-g", "std"]
}
```

## Files

这组设置主要用于文件监听排障。选择哪些 RTL 文件属于项目，优先写在 `vide.toml` 的 `sources` / `exclude` 中。

对应日常使用页：[语言识别](../../user-guide/daily-use/language-support/)；项目文件选择见 [项目配置](../../user-guide/project-configuration/)。

<VscodeSettingsTable locale="zh" settings={['vide.files.excludeDirs', 'vide.files.watcher']} />

`client` 会优先使用 VS Code 文件变化通知。客户端不支持动态监听文件时会回退到服务端监听；`notify` 和 `server` 都走服务端监听路径。

## Workspace

<VscodeSettingsTable locale="zh" settings={['vide.workspace.auto.reload']} />

## Scope

这组设置会影响跳转、引用、重命名等阅读能力；日常使用保持默认值即可。

对应日常使用页：[导航和阅读](../../user-guide/daily-use/navigation/)。

<VscodeSettingsTable locale="zh" settings={['vide.scope.visibility']} />

这个设置会影响引用搜索、重命名和当前文件高亮。

## Formatter 和 Formatting

配置格式化时看这组。Vide 会调用外部 `verible-verilog-format`。

对应日常使用页：[格式化](../../user-guide/daily-use/formatting/)。

<ThinLinkCard
  href="https://github.com/chipsalliance/verible#installation"
  title="Verible 安装说明"
  action="打开"
>
  安装或更新外部 <code>verible-verilog-format</code> 工具。
</ThinLinkCard>

<ThinLinkCard
  href="https://chipsalliance.github.io/verible/verilog_format.html"
  title="verible-verilog-format 参数文档"
  action="打开"
>
  配置 <code>vide.formatter.args</code> 前先对照这里的参数。
</ThinLinkCard>

<VscodeSettingsTable
  locale="zh"
  settings={[
    'vide.formatter.provider',
    'vide.formatter.path',
    'vide.formatter.args',
    'vide.formatting.on.enter',
    'vide.formatting.in.comments',
    'vide.formatting.indent.width',
  ]}
/>

`verible` 格式化后端会在自定义参数后追加当前缩进宽度对应的 `--indentation_spaces=<N>`。

## Inlay Hints

行内提示会直接显示在编辑器里，适合日常阅读端口和参数连接。

对应日常使用页：[结构辅助](../../user-guide/daily-use/structure/)。

<VscodeSettingsTable
  locale="zh"
  settings={[
    'vide.inlayHints.port.connection.enable',
    'vide.inlayHints.parameter.assignment.enable',
    'vide.inlayHints.end.structure.enable',
  ]}
/>

## Lens

实例数量提示会显示在模块声明上方。

对应日常使用页：[结构辅助](../../user-guide/daily-use/structure/)。

<VscodeSettingsTable locale="zh" settings={['vide.lens.instantiations.enable']} />

## Semantic Tokens

语义高亮会在主题支持时让端口方向、时钟复位和读写位置更容易区分。

对应日常使用页：[语言识别](../../user-guide/daily-use/language-support/) 和 [结构辅助](../../user-guide/daily-use/structure/)。

<VscodeSettingsTable
  locale="zh"
  settings={['vide.semantic.tokens.port.clk.rst.enable', 'vide.semantic.tokens.port.input.output.enable']}
/>

## Diagnostics

诊断会显示在 VS Code 的 `Problems` 面板和编辑器下划线里。

对应日常使用页：[诊断](../../user-guide/daily-use/diagnostics/)。

<VscodeSettingsTable
  locale="zh"
  settings={[
    'vide.diagnostics.enable',
    'vide.diagnostics.update',
    'vide.diagnostics.parse.enable',
    'vide.diagnostics.semantic.enable',
    'vide.diagnostics.slang.warnings',
    'vide.diagnostics.slang.rules',
  ]}
/>

`vide.diagnostics.slang.warnings` 对齐 slang `-W...` 语义，但在 VS Code 设置里不写前导 `-W`。`vide.diagnostics.slang.rules` 的选择器支持 `code:<subsystem>:<code>`、`option:<name>`、`group:<name>`、`source:parse`、`source:semantic`；`severity` 可选 `ignore`、`info`、`warning`、`error`、`fatal`。

示例：

```json
{
  "vide.diagnostics.slang.rules": [
    { "selector": "source:parse", "severity": "warning" },
    { "selector": "option:unconnected-port", "severity": "ignore" }
  ]
}
```

## Signature Help

签名帮助用于实例端口连接和参数赋值列表。

对应日常使用页：[签名帮助](../../user-guide/daily-use/signature-help/)。

<VscodeSettingsTable locale="zh" settings={['vide.signature.help.params.only']} />
