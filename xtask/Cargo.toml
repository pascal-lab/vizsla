[package]
name = "xtask"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
anyhow.workspace = true
project-model = { workspace = true, features = ["manifest-schema"] }
serde_json.workspace = true
vide = { path = "..", features = ["user-config-schema"] }
